import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  container: {
    backgroundColor: "#977d0a",
    height: 40,
    width: 150,
    paddingHorizontal: 20,
    borderRadius: 10,
    color: "#fff",
    fontWeight: "bold",
    alignItems: 'center',
    justifyContent: 'center',
    display: 'flex',
    textAlign:'center',
  },
});
